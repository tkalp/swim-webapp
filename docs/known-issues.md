# Known Issues & Tech Debt

> Inventory from codebase audit. Severity: critical (breaks functionality), significant (wrong behavior), minor (dead code/cosmetic).
> Last updated: 2026-03-15 (Phase 2 complete)

| ID | Severity | Feature | Location | Description | Status |
|----|----------|---------|----------|-------------|--------|
| 1 | critical | Training | `backend/app/routes/training_sessions.py` | `create_session` upsert compared `DateTime(timezone=True)` column to raw VARCHAR string — PostgreSQL error: `operator does not exist: timestamp with time zone = character varying`. Fixed by parsing ISO strings via `_parse_iso_datetime()`. | fixed |
| 2 | critical | Training | `backend/app/services/session_service.py` | Same timestamp-vs-string comparison in `materialize_session` (line 35) and `cleanup_virtual_sessions` (line 95). Fixed by adding `_parse_iso()` helper and using `datetime.now(timezone.utc)`. | fixed |
| 3 | critical | Training | `backend/app/infrastructure/models.py` + `backend/app/routes/training_sessions.py` + `frontend/src/services/sessionService.ts` | `day_of_week` stored as Integer in DB, compared as string ("Monday") in backend virtual sessions and frontend `createSessionsFromSchedules`. "Create from Schedule" always produces 0 sessions. | fixed |
| 4 | significant | Training | `frontend/src/components/squad/SessionsList.tsx:683` | Sessions list reads `session.workout_template?.name` but API returns flat `workout_name` string. Workout badges never render. | fixed |
| 5 | significant | Training | `frontend/src/components/squad/SessionsList.tsx:325,333` | `training_type` hardcoded to `"Swim"` in `handleSubmitSession`, ignoring form data. | fixed |
| 6 | minor | Training | `frontend/src/services/sessionService.ts:11` | TS type has `created_by` field that doesn't exist on the DB model. | open |
| 7 | minor | Training | `frontend/src/components/squad/sessions/CreateFromScheduleModal.tsx:46` | `new Date("yyyy-MM-dd")` parsed as UTC midnight — `getDay()` returns wrong day in timezones behind UTC. | open |
| 8 | critical | Auth | `backend/app/services/auth_service.py:204-229` | Password reset emails are not sent. `forgot_password()` prints reset link to console via `print()`. Users cannot reset passwords in production. | fixed |
| 9 | critical | Auth | `backend/app/services/auth_service.py:232-251` | Password reset does not revoke existing refresh tokens. Stolen refresh tokens remain valid after password reset. | fixed |
| 10 | significant | Auth | `frontend/src/stores/authStore.ts` | Tokens stored in `localStorage` (vulnerable to XSS). `httpOnly` cookies would be more secure. | open |
| 11 | significant | Auth | `backend/app/services/auth_service.py` | No password validation on backend. Frontend enforces 8+ chars with complexity, but backend accepts any string including empty. | open |
| 12 | significant | Auth | `backend/app/services/auth_service.py:221` | Password reset JWT uses same `JWT_SECRET` as access tokens. Should use separate secret or DB-stored random token. | open |
| 13 | significant | Auth | `backend/app/services/auth_service.py` | Revoked/expired refresh tokens never deleted from DB. Table grows unboundedly. | open |
| 14 | minor | Auth | `frontend/src/stores/authStore.ts:312-313` | `refreshSession()` reuses existing user object — stale profile data persists until page reload. | open |
| 15 | minor | Auth | `frontend/src/lib/apiClient.ts:76` | Hard redirect via `window.location.href = '/login'` on refresh failure bypasses React Router, loses unsaved UI state. | open |
| 16 | minor | Auth | `frontend/src/lib/apiClient.ts` + `frontend/src/stores/authStore.ts` | Duplicate refresh logic in apiClient and authStore. Race condition if two 401s arrive simultaneously — both attempt refresh, one revokes the token, other fails and logs user out. | open |
| 17 | minor | Auth | `frontend/src/stores/__tests__/authStore.test.ts` | Auth store tests still reference Supabase mocks (`mockSupabaseClient.auth.signInWithPassword`). Tests are broken. | open |
| 18 | minor | Auth | Multiple frontend files | Stale Supabase references in comments across ~15 files. `PrivacyPolicy.tsx` tells users data is on "Supabase's secure cloud infrastructure" — no longer accurate. | open |
| 19 | minor | Auth | `frontend/src/contexts/AuthContext.tsx` | `signUp` not exposed through AuthContext — unreachable from components using `useAuth()`. No signup page exists. | open |
| 20 | minor | Auth | `backend/app/services/auth_service.py:17` | Default JWT secrets hardcoded (`"dev-secret-change-me"`). Middleware checks for env var but auth_service silently uses fallback. | open |
| 21 | critical | Squads | `backend/app/routes/squad_crud.py:130-178` | **Authorization bypass:** `update_squad()` and `delete_squad()` only check authentication, not squad membership/role. Any authenticated user can update or delete any squad. `AuthorizationService.verify_squad_ownership()` exists but is not called. | fixed |
| 22 | critical | Squads | `backend/app/routes/squad_crud.py:88` | **Authorization bypass:** `get_squad()` returns any squad by ID to any authenticated user. No membership check. Same issue affects sub-resource endpoints (`/swimmers`, `/schedules`, `/sessions`, `/calendar-events`). | fixed |
| 23 | critical | Squads | `backend/app/routes/permissions.py:85-119` | **Authorization bypass:** `update_permission()` and `remove_coach_from_squad()` allow any authenticated user to modify/remove any coach's squad membership. No owner/admin check. | fixed |
| 24 | significant | Squads | `backend/app/routes/permissions.py:163-179` | Any authenticated user can invite anyone to any squad. No check that inviter is owner/admin. | fixed |
| 25 | significant | Squads | `backend/app/routes/permissions.py:204-209` | Invitation accept hardcodes `role="assistant"` with `can_manage_swimmers=False`, ignoring the requested role. "assistant" is not in the defined role set (owner/admin/member). | fixed |
| 26 | significant | Squads | `backend/app/infrastructure/models.py` vs `frontend/src/services/permissionService.ts` | Permission model mismatch: DB only has `role` + `can_manage_swimmers`. Frontend assumes 11 permission fields. For `member` role, permissions will be undefined. Masked for owner/admin by hardcoded defaults. | fixed |
| 27 | significant | Squads | `backend/app/routes/coach_connections.py:187-225` | Coach search queries `Profile.email` and `Profile.first_name` but Profile model only has `id`, `user_id`, `created_at`. Will crash with `AttributeError` at runtime — coach connection search is broken. | fixed |
| 28 | significant | Squads | `backend/app/routes/permissions.py:220-236` | No recipient verification on invitation decline. Any user can decline any invitation by ID. | fixed |
| 29 | significant | Squads | `frontend/src/services/coachService.ts:39` | Pending connection requests always returns `[]`. No backend endpoint returns pending requests received by user — coaches can't see incoming requests. | open |
| 30 | significant | Squads | `backend/app/routes/coach_connections.py:65-98` | No duplicate connection check. Multiple pending/accepted connections can be created between same coaches. | fixed |
| 31 | significant | Squads | `backend/app/routes/squads.py` (all endpoints) | No squad membership verification on analytics. Any authenticated user can access metrics/performance/rankings for any squad. | fixed |
| 32 | minor | Squads | `frontend/src/hooks/api/useSquadApi.ts:17`, `useCoachApi.ts:14` | Stale Supabase references in JSDoc comments. | fixed |
| 33 | minor | Squads | `frontend/src/pages/AddSquad.tsx:25` | `console.log` left in production code for squad creation. | open |
| 34 | minor | Squads | `frontend/src/pages/AddSquad.tsx` + `EditSquad.tsx` | Redundant standalone pages duplicate modal functionality. AddSquad bypasses store sync causing stale state. | open |
| 35 | minor | Squads | `frontend/src/hooks/useSquadData.ts` | Deprecated hook still importable. Logic duplicated inline in `Squad.tsx`. | fixed |
| 36 | critical | Swimmers | SwimRankings.net | Cloudflare Turnstile blocks all scraping. CurlFetcher/HttpxFetcher can't solve challenges. PlaywrightFetcher can't solve Turnstile CAPTCHA. ScraperAPI/ScrapingBee can't render the pages. Sync pipeline is non-functional. | open |
| 37 | significant | Swimmers | `backend/app/routes/swimmers/core.py` | Missing authorization on `PUT /swimmers/{id}/update` and `DELETE /swimmers/{id}/delete`. Any authenticated user can modify/delete any swimmer by UUID. | fixed |
| 38 | significant | Swimmers | `backend/app/routes/swimmers/core.py` | `POST /swimmers/with-external-link` does not check for duplicate external athlete IDs. Multiple swimmers can be linked to the same SwimRankings athlete. | open |
| 39 | significant | Swimmers | `worker/sync/orchestrator.py` | Sync cancellation is best-effort: backend sets `sync_status='cancelled'` but worker task doesn't check this during execution. `check_cancellation_fn` exists but isn't wired up. | open |
| 40 | significant | Swimmers | `backend/app/routes/swimmers/sync.py` | No database-level lock on sync trigger. Two simultaneous requests can both pass the `in_progress` check and enqueue duplicate Celery tasks. | open |
| 41 | significant | Swimmers | `frontend/src/pages/SwimmerPage.tsx` | After sync completes, frontend does not invalidate React Query cache for best times/FINA points. Stale data shown until user navigates away or cache expires (5min). | open |
| 42 | significant | Swimmers | `worker/sync/orchestrator.py` | `_fetch_and_insert_splits()` is a placeholder — logs "not yet implemented" and returns. Incremental syncs never get splits for new results >50m. | open |
| 43 | minor | Swimmers | `backend/app/services/swimmer_service.py` | Legacy Supabase-era service with synchronous patterns. Routes in `core.py` bypass it and use async SQLAlchemy directly. The `get_swimmer` endpoint (without `/basic-info`) still goes through this legacy service. | open |
| 44 | minor | Swimmers | `worker/database.py` | Worker uses separate synchronous DB connection (psycopg2). Not part of backend's connection pool or transaction management. | open |
| 45 | minor | Swimmers | `backend/app/routes/swimrankings.py` | In-memory search cache (`_search_cache` dict) is process-local. In multi-worker deployments, each worker has its own cache. | open |
| 46 | significant | Workouts | `backend/app/infrastructure/models.py:317` | `WorkoutTemplate.usage_count` declared as `DateTime` column but named/used as a counter. Actual usage count comes from runtime COUNT query — column serves no purpose in current type. | open |
| 47 | significant | Workouts | `backend/app/routes/workout_ratings.py` | `effectiveness_rating` and `rating_count` on `WorkoutTemplate` are NEVER updated by rating handlers. No trigger or recalculation exists. Rating stars always show 0 for new workouts. | fixed |
| 48 | significant | Workouts | `backend/app/routes/workouts.py` | No ownership check on create/read/update/delete. Any authenticated coach can modify any workout by ID. Only `PUT /workout-sharing/visibility` enforces ownership. | fixed |
| 49 | significant | Workouts | `backend/app/routes/workout_tags.py` | ALL tag endpoints (CRUD + associations) lack `Depends(get_current_user_id)`. Tags are publicly accessible without authentication. | fixed |
| 50 | significant | Workouts | `backend/app/routes/workout_analysis.py:91` | `POST /workout-analysis/quick-stats` omits auth dependency — publicly accessible. | fixed |
| 51 | significant | Workouts | `backend/app/routes/workout_sharing.py:76` | Clone does not copy tag associations from original. Coach loses all tags silently. Internal duplicate (`POST /workouts/{id}/duplicate`) does copy tags. | open |
| 52 | significant | Workouts | `frontend/src/pages/WorkoutsLibrary.tsx:596-608` | "Load All Workouts" loop reads `hasMore` from stale closure — may run indefinitely or terminate prematurely. | open |
| 53 | significant | Workouts | `frontend/src/pages/WorkoutView.tsx:109-112` | Delete handler only logs to console and navigates — does NOT call API. Clicking Delete doesn't actually delete the workout. | fixed |
| 54 | minor | Workouts | `backend/app/routes/workout_results.py:163-178` | Response serializes `result_units` into both `units` and `result_units` keys. `units` contains course code ("SCM") not measurement unit. | open |
| 55 | minor | Workouts | `frontend/src/pages/WorkoutsLibrary.tsx:209-219` | Duplicate error path uses `alert()` instead of `showToast` pattern used elsewhere. | open |
| 56 | minor | Workouts | `backend/app/routes/workout_sharing.py:154-211` | `network` visibility applies no connection filter. Any coach passing `visibility=network` sees all network-visibility workouts from all coaches. | open |
| 57 | significant | Training | `backend/app/routes/practice_notes.py` + `frontend/src/services/practiceNotesService.ts` | Practice notes always POST (INSERT) instead of upsert. Each save creates a duplicate note row. PUT endpoints exist on backend but frontend never calls them. GET returns oldest note only. | fixed |
| 58 | significant | Training | `frontend/src/components/squad/sessions/PracticeNotesModal.tsx` | Rich form fields (announcements, focus, ratings, highlights, etc.) are collected but only `{ training_session_id, notes }` is sent to API. DB schema also only has a single `notes: Text` column. All structured data silently discarded. | open |
| 59 | significant | Training | `frontend/src/components/squad/sessions/AttendanceModal.tsx` | `notes` field shown in UI but `TrainingAttendance` ORM model has no `notes` column. Value accepted in request body but never persisted. | open |
| 60 | significant | Training | `frontend/src/components/squad/sessions/AttendanceModal.tsx` | `handleMarkRemainingAbsent` is client-side only. Backend endpoint `POST /attendance/mark-remaining-absent` exists but is never called. | open |
| 61 | significant | Training | `backend/app/routes/training_sessions.py` | `GET /training-sessions/virtual` endpoint (computes virtual sessions from schedules) is never called by any frontend service. Coaches never see virtual sessions. Frontend only calls `/squads/{id}/sessions` which returns materialized sessions only. | open |
| 62 | significant | Training | `backend/app/routes/training_sessions.py` + all training routes | No squad membership verification. Any authenticated user can create/edit/delete sessions, attendance, and notes in any squad. | fixed |
| 63 | significant | Training | `backend/app/services/session_service.py:109` | `cleanup_virtual_sessions` queries ALL attendance records across ALL squads (no WHERE clause scoping to squad). Loads entire attendance table into memory. | open |
| 64 | minor | Training | `backend/app/routes/training_sessions.py` | `SessionCreate` allows null `start_date`/`end_date`. API can create sessions with null timestamps, breaking date-based filtering. | open |
| 65 | minor | Training | `frontend/src/components/squad/SessionsList.tsx:168-182` | "Next week" date range off-by-one on Sundays: computes start 8 days ahead instead of 1 day. | open |
| 66 | minor | Training | `backend/app/routes/training_sessions.py` | Virtual session deduplication uses UTC date key. Timezone edge case: late-night local session becomes next-day UTC, causing duplicate virtual session alongside materialized one. | open |
| 67 | minor | Training | `frontend/src/components/squad/sessions/CreateFromScheduleModal.tsx` | Bypasses Zustand store — calls `sessionService` directly, then triggers full page refresh via `onRefresh`. | open |
| 68 | minor | Training | `frontend/src/components/squad/ScheduleTab.tsx` | Misleading name — this is a calendar events view (`CalendarEvent`), not training schedule management. | open |
| 69 | significant | Performance | `backend/app/services/prediction/time_utils.py` vs `backend/app/routes/swimmers/predictions.py` | Dual conversion-factor tables with different values. `PoolConverter` uses {200m: 3.0%, 400m: 3.5%, 800m: 3.8%, 1500m: 4.0%}. `predictions.py` uses {200m: 3.5%, 400m: 4.5%, 800m+: 5.0%}. Best times and predictions use different conversion assumptions. | open |
| 70 | significant | Performance | `frontend/src/pages/SwimmerComparison.tsx` | Predictions section exists in backend response but is never rendered in JSX. `expandedSections.predictions` state exists but no corresponding UI section. Backend race prediction calculation is wasted. | open |
| 71 | significant | Performance | `backend/app/services/prediction/achievement_validator.py` | Fixed 5-sample cap: sliding window iterates all windows but stores only first 5, then computes `achievement_rate` from those 5. Inaccurate for swimmers with many results. | open |
| 72 | significant | Performance | `frontend/src/pages/SquadPerformance.tsx` | Does not use the `useSquadPerformance` React Query hook that exists for it. Uses bare `useEffect` + local state, losing caching, background refetch, and deduplication. | open |
| 73 | minor | Performance | `frontend/src/hooks/useSquadBenchmarks.ts:38` | Hardcoded swimmer UUID (`7c8fccee-...`) labelled "Kazeo Lee" in debug `console.log`. Debugging residue. | open |
| 74 | minor | Performance | `backend/app/services/trend_analysis_service.py` | `_calculate_catch_up_years()` always returns `None` (stub). `ComparativeTrend.catch_up_years` is always None. | open |
| 75 | minor | Performance | `frontend/src/components/swimmers/FinaPointsTab.tsx` | Gender defaulting: `swimmer.sex === "Female"` -> "female", everything else (including null) -> "male". No explicit handling for missing data. | open |
| 76 | minor | Performance | `backend/app/services/prediction_service.py` | Top-level shim file is dead code — pre-refactor standalone version with duplicate dataclasses. All routes import from `prediction/` package. Should be deleted. | fixed |
| 77 | significant | Calendar | `backend/app/routes/calendar.py` | No ownership/authorization check. Any authenticated user can update or delete calendar events for any squad. | fixed |
| 78 | significant | Calendar | `frontend/src/hooks/useNotifications.ts` | `markAsRead`/`markAllAsRead` are client-side only. Every 30s poll resets all items to unread. No backend persistence for read state. | open |
| 79 | significant | Calendar | `frontend/src/pages/Calendar.tsx` | Creating new events sends `squad_id: ''` (empty string) because `squadId` prop is never passed to `EventFormModal`. Backend will fail or insert invalid UUID. | open |
| 80 | minor | Calendar | `frontend/src/hooks/useNotifications.ts` | `connection_accepted` type declared but never produced by backend. | open |
| 81 | minor | Calendar | `backend/app/routes/calendar.py:64` | Date filter `end_date <= to_date` excludes multi-day events spanning month boundaries. | open |
| 82 | minor | Calendar | `frontend/src/stores/calendarStore.ts` | Leftover `console.log` in `setSelectedDate` and `openFormWithDate`. | open |
| 83 | critical | Admin | `backend/app/routes/beta.py:89-148` | ALL `/beta/admin/*` routes have ZERO authentication. Any unauthenticated caller can list all beta submissions, change status, or overwrite notes. | removed (beta deleted) |
| 84 | significant | Admin | `backend/app/routes/admin.py:93` | Admin check is hardcoded single email. No DB-backed role system. | open |
| 85 | significant | Admin | `backend/app/routes/admin.py` | `start_bulk_sync` returns `job_id: None`. Frontend relies on 2s timeout + history scan to find the job — race condition. | open |
| 86 | significant | Admin | `backend/app/routes/beta.py:66-68` | Email notifications (confirmation + admin alert) are TODO stubs. Applicants receive no acknowledgment. | removed (beta deleted) |
| 87 | significant | Admin | `frontend/src/pages/AdminBetaWaitlist.tsx` | `sendApprovalEmail` is a stub calling `alert()`. No email sent. | removed (beta deleted) |
| 88 | minor | Admin | `backend/app/routes/admin.py` | Uses deprecated `datetime.utcnow()` instead of `datetime.now(timezone.utc)`. | open |
| 89 | minor | Admin | `backend/app/routes/beta.py:80` | Duplicate email detection via string matching on exception. `BetaWaitlist` has no UNIQUE constraint on email — 409 may never trigger. | removed (beta deleted) |
| 90 | minor | Admin | `frontend/src/pages/AdminBetaWaitlist.tsx` | No frontend route guard. Any authenticated user can access `/admin/beta-waitlist`. | removed (beta deleted) |
| 91 | significant | AI Coach | `backend/app/services/chroma_service.py:425` | `generate_workout` is synchronous, blocks FastAPI event loop 5-15s per request. Should use `run_in_executor`. | fixed |
| 92 | significant | AI Coach | `backend/app/services/chroma_service.py:349-400` | Creates new `Anthropic` client on every request instead of reusing module-level instance. | open |
| 93 | minor | AI Coach | `backend/app/services/chroma_service.py:424` | `num_examples = 6` hardcoded. May error if ChromaDB collection has fewer than 6 documents. | open |
| 94 | minor | AI Coach | `backend/app/models/schemas.py:20` | `GenerateWorkoutResponse.examples` always None — kept for backwards compatibility but never populated. | open |
| 95 | minor | AI Coach | `backend/app/services/chroma_service.py:121-137` | `parse_time_to_seconds` raises unhandled `ValueError` for invalid time strings, propagating through `build_athlete_context`. | open |
| 96 | minor | AI Coach | `backend/app/services/chroma_service.py:278,316` | `print()` statements in production code alongside proper `logger` calls. | open |
| 97 | minor | AI Coach | `frontend/src/services/aiCoachService.ts:82-88` | `checkAPIHealth()` calls `/health` which may not exist as a registered route. | open |
| 98 | significant | Standards | `backend/app/routes/time_standards.py:243-277` | No ownership check on individual standard update/delete. Any authenticated user can modify any standard. | fixed |
| 99 | significant | Standards | `backend/app/routes/time_standards.py:230-240` | Bulk import has no ownership check on `set_id`. Any user can bulk-insert into any set. | fixed |
| 100 | significant | Standards | `backend/app/routes/time_standards.py:297-328` | Tracking POST/DELETE has no verification caller has access to swimmer or standard set. | fixed |
| 101 | significant | Standards | `backend/app/routes/time_standards.py:89-113` | N+1 query: one extra `SELECT COUNT(*)` per set on list endpoint. | open |
| 102 | minor | Standards | `frontend/src/services/swimmerStandards.ts:55` | `getStandardsForEvent` fetches ALL standards for entire set every call, filters client-side. | open |
| 103 | minor | Standards | `frontend/src/services/timeStandards.ts:76` | `set.organization.toLowerCase()` without null check — `organization` is optional, will crash for null. | open |
| 104 | minor | Standards | `backend/app/routes/time_standards.py:266-277` | DELETE silently succeeds for non-existent IDs — no 404. | open |
| 105 | minor | Standards | `frontend/src/services/timeStandards.ts:86-104` | CSV batch errors count entire batch of 100 as failed even if one row is invalid. | open |
